[pytest]
pythonpath = .
# evals/ carries tests for the eval harness itself. The harness is test infrastructure,
# but an eval built on a broken device double measures the double, not the agent - so it
# runs in the normal suite alongside everything else.
testpaths = tests evals
